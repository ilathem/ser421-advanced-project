---
import { TodoComponent } from './angular-demo';

const todoText = "I need to do something";
---
<TodoComponent client:visible />
<TodoComponent client:visible todoText="I don't want to do anything" />
<TodoComponent client:visible todoText={todoText} />
